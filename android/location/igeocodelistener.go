package location

import (
	"context"
	"fmt"
	"github.com/xaionaro-go/binder/binder"
	"github.com/xaionaro-go/binder/parcel"
)

// Code generated by aidlgen. DO NOT EDIT.

const DescriptorIGeocodeListener = "android.location.IGeocodeListener"

const (
	TransactionIGeocodeListenerOnResults = binder.FirstCallTransaction + 0
)

const (
	MethodIGeocodeListenerOnResults = "onResults"
)

type IGeocodeListener interface {
	AsBinder() binder.IBinder
	OnResults(ctx context.Context, error_ string, results []Address) error
}

type GeocodeListenerProxy struct {
	Remote binder.IBinder
}

func NewGeocodeListenerProxy(
	remote binder.IBinder,
) *GeocodeListenerProxy {
	return &GeocodeListenerProxy{Remote: remote}
}

func (p *GeocodeListenerProxy) AsBinder() binder.IBinder {
	return p.Remote
}

var _ IGeocodeListener = (*GeocodeListenerProxy)(nil)

func (p *GeocodeListenerProxy) OnResults(
	ctx context.Context,
	error_ string,
	results []Address,
) error {
	_data := parcel.New()
	_data.WriteInterfaceToken(DescriptorIGeocodeListener)
	_data.WriteString16(error_)
	if results == nil {
		_data.WriteInt32(-1)
	} else {
		_data.WriteInt32(int32(len(results)))
		for _, _item := range results {
			_data.WriteInt32(1)
			if _err := _item.MarshalParcel(_data); _err != nil {
				return _err
			}
		}
	}

	_code, _err := p.Remote.ResolveCode(ctx, DescriptorIGeocodeListener, MethodIGeocodeListenerOnResults)
	if _err != nil {
		return fmt.Errorf("resolving %s.%s: %w", DescriptorIGeocodeListener, MethodIGeocodeListenerOnResults, _err)
	}

	_, _err = p.Remote.Transact(ctx, _code, binder.FlagOneway, _data)
	return _err
}

// GeocodeListenerStub dispatches incoming binder transactions
// to a typed IGeocodeListener implementation.
type GeocodeListenerStub struct {
	Impl IGeocodeListener
}

var _ binder.TransactionReceiver = (*GeocodeListenerStub)(nil)

func (s *GeocodeListenerStub) Descriptor() string {
	return DescriptorIGeocodeListener
}

func (s *GeocodeListenerStub) OnTransaction(
	ctx context.Context,
	code binder.TransactionCode,
	_data *parcel.Parcel,
) (*parcel.Parcel, error) {
	switch code {
	case TransactionIGeocodeListenerOnResults:
		if _, _err := _data.ReadString16(); _err != nil {
			return nil, _err
		}
		_arg_error_, _err := _data.ReadString16()
		if _err != nil {
			return nil, _err
		}
		// TODO: array/list param unmarshaling not yet supported in stubs
		var _arg_results []Address
		_ = _arg_results
		_err = s.Impl.OnResults(ctx, _arg_error_, _arg_results)
		_ = _err
		return nil, nil
	default:
		return nil, fmt.Errorf("unknown transaction code %d", code)
	}
}

// IGeocodeListenerServer is the server-side interface that user implementations
// provide to NewGeocodeListenerStub. It contains only the business methods,
// without AsBinder (which is provided by the stub itself).
type IGeocodeListenerServer interface {
	OnResults(ctx context.Context, error_ string, results []Address) error
}

type geocodeListenerStubWrapper struct {
	impl       IGeocodeListenerServer
	stubBinder *binder.StubBinder
}

func (w *geocodeListenerStubWrapper) AsBinder() binder.IBinder {
	return w.stubBinder
}

func (w *geocodeListenerStubWrapper) OnResults(
	ctx context.Context,
	error_ string,
	results []Address,
) error {
	return w.impl.OnResults(ctx, error_, results)
}

var _ IGeocodeListener = (*geocodeListenerStubWrapper)(nil)

// NewGeocodeListenerStub creates a server-side IGeocodeListener wrapping the given
// server implementation. The returned value satisfies IGeocodeListener
// and can be passed to proxy methods; its AsBinder() returns a
// *binder.StubBinder that is auto-registered with the binder
// driver on first use.
func NewGeocodeListenerStub(
	impl IGeocodeListenerServer,
) IGeocodeListener {
	wrapper := &geocodeListenerStubWrapper{impl: impl}
	stub := &GeocodeListenerStub{Impl: wrapper}
	wrapper.stubBinder = binder.NewStubBinder(stub)
	return wrapper
}
